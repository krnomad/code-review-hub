/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Tailwind가 스캔할 파일 경로
  theme: {
    extend: {}, // 사용자 정의 테마 설정
  },
  plugins: [], // 추가 플러그인 설정
};
